<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CCCC" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- Прозрачный ActionBar -->
        <item name="windowActionBar">false</item> <!-- Отключаем стандартный ActionBar -->
        <item name="windowNoTitle">true</item> <!-- Отключаем заголовок -->
        <item name="android:windowBackground">@android:color/transparent</item> <!-- Прозрачный фон -->
    </style>

    <style name="Theme.CCCC" parent="Base.Theme.CCCC">
        <!-- Дополнительные кастомизации можно добавить здесь -->
    </style>
</resources>
